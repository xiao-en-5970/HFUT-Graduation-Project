#!/bin/bash
# 复制为 run.sh 并填入本地环境变量，不提交到 Git
# cp run.sh.example run.sh && chmod +x run.sh

# 手动设置环境变量（参考 .env.example）
# export DB_HOST=localhost
# export DB_PASSWORD=xxx
# export JWT_SECRET=xxx
# ...

exec go run .
